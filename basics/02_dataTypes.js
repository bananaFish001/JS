"Use Strict" // treat all js code as newer version

// alert("hello") = This alert keyword can be used in browser but here while running in node it will give you error.

console.log(3 + 3)

let name = "Raj Shekhar Singh"
let age = "22"
let isLoggedIn = true
let state

// JS datatypes:
// number - range(2^53)
// bigInt
// String
// boolean
//null - It is datatype but also a stand alone value.
//undefined - Value of a declared variable is not defined.
//symbol - when we deal with unique values

console.log(typeof"age")
